  <%= provide(:title, "Plan") %>
<div class="container" style="max-width: 970px">
  <div>
    <div class="page-header">
      <h1>Registro <small>Tipo de Plan</small></h1>
      <div class="progress">
        <div class="progress-bar progress-bar-info progress-bar-striped" role="progressbar" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100" style="width: 25%">
        </div>
      </div>
    </div>
  </div>
  <div>
    <div class="row">
      <% @plans.each do |plan| %>
        <div class="col-md-4">
          <div class="panel panel-success">
            <div class="panel-heading">
              <h4 class="text-center"><%= plan.name %></h4>
            </div>
            <div class="panel-body">
              <p class="lead text-center">
                <strong>
                <%= number_to_currency(plan.cost, :unit => "$") %>/año
                </strong>
              </p>
              <%= markdown(plan.description) %>
            </div>
            <div class="panel-footer">
            <% #Plan 1 plan mas bajo %>
              <%= link_to "Elegir", new_plan_hospital_path(plan.id), 
                                class: "btn btn-lg btn-block btn-success" %> 
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="page-header">
      <h3>Nota</h3>
    </div>
    <div>
      <ul>
        <li>Cantidades en Pesos Mexicanos.</li>
        <li>Precios preferenciales por confiar en nosotros en nuesto inicio.</li>
        <li>Al registrarse se le enviara un correo con la información de pago.</li>
        <li>Se tiene un tiempo de gracia para realizar el pago.</li>
      </ul>
    </div>
  </div>
</div>