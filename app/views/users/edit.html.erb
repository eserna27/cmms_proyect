<%= provide(:title, "Usuario" ) %>
  <div class="page-header">
    <h2>
    	Edición <small><%=  @user.name %></small>
    </h2>
  </div>
<div class="jumbotron" style="padding: 25px 0;">
  <div class="container" style="margin-top: 0;">
  <div class="col-xs-13 col-md-9">
	  <div class="modal-body">
		 <h2 class="form-signin-heading">Teclee los datos</h2>
		  <%= form_for(@user)  do |f| %>
		    <%= render 'shared/error_messages', object: f.object %>
		    <%= f.text_field :name, class: "form-control", placeholder: "Nombre",
		                                            required: true, autofocus: true, style: "margin-bottom: 10px;" %>          
		    <%= f.text_field :email, class: "form-control", placeholder: "Email",
		                                            required: true, style: "margin-bottom: 10px;" %>
		    <%= f.password_field :password, class: "form-control", placeholder: "Contraseña",
		                                              required: true, style: "margin-bottom: 10px;" %>  
		    <%= f.password_field :password_confirmation, class: "form-control", 
		                                      placeholder: "Confirmación Contraseña", style: "margin-bottom: 10px;" %>  
		    <%= f.hidden_field :hospital_id, :value => @hospital.id %>
		     
		    </div>
	        <div class="modal-footer">
	          <%= f.submit "Guardar", class: "btn btn-primary" %>
	        </div>
		  <% end %>
		</div>
	</div>
</div>